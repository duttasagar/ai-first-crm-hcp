import { Box, Typography } from "@mui/material";

export default function Dashboard() {
  return (
    <Box p={4}>
      <Typography variant="h4">
        Dashboard
      </Typography>
    </Box>
  );
}